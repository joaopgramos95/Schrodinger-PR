import «_work_tepsconv»

/-!
# Zero-safe critical convergence

The implementation lives in the project-root module `_work_tepsconv` while
the final trace proof is being assembled.  This stable library module exposes
its proved `H^{1/2}` convergence results to the dependency graph.
-/
